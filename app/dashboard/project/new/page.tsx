import { ProjectNewView } from "./ui/projectNew.view"

export const metadata = {
    title: 'Nuevo proyecto | Proyectos'
}

export default async function ProjectNewPage() {
    return <ProjectNewView />
}